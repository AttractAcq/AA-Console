# Supabase — AA Console (project ref: bancbdztffokwiifzoiv)
# The publishable key is safe to ship in the frontend; RLS enforces access.
VITE_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxx

# Agent runtime base URL, for the admin-only Master AI chat.
# Local: http://localhost:8787   Deployed: your Railway service URL.
VITE_AGENT_RUNTIME_URL=http://localhost:8787
